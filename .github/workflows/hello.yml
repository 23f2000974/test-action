name: Hello World Action

on:
  workflow_dispatch:   # lets you trigger it manually with a button
  push:                 # also runs whenever you push code
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 23f2000974@ds.study.iitm.ac.in
        run: echo "Hello, world!"
